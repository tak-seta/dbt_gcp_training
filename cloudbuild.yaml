steps:
  # dbtを実行するためのDockerfileのコンテナイメージのビルド
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "us-west2-docker.pkg.dev/$PROJECT_ID/dbt-docker-repo/dbt-transform:tag1",
        # "--build-arg",
        # "env=$_ENV",
        "."
      ]
  # GARにpush
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "us-west2-docker.pkg.dev/$PROJECT_ID/dbt-docker-repo/dbt-transform:tag1"]
  # Cloud Run Jobsにデプロイ(定期的にデプロイする都合上updateにしているので事前にコンソールから同名同リージョンのjobを定義しておくと良いです)
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "beta",
        "run",
        "jobs",
        "update",
        "dbt-transform",
        "--image",
        "us-west2-docker.pkg.dev/$PROJECT_ID/dbt-docker-repo/dbt-transform:tag1",
        "--command",
        "bash",
        "--args",
        "scripts/dbt_build.sh",
        "--region",
        "asia-northeast1",
        "--max-retries",
        "1",
        "--task-timeout",
        "3600s",
      ]
images:
  - "us-west2-docker.pkg.dev/$PROJECT_ID/dbt-docker-repo/dbt-transform:tag1"
options:
  # Cloud BuildはデフォルトでCloud Storageにビルドログを保存しようとするが、
  # Cloud Loggingだけにするように指定
  logging: CLOUD_LOGGING_ONLY
